---
import { Icon } from "astro-icon/components";

export interface Props {
	bgColour?: string;
	linkColour?: string;
	dividerColour?: string;
	logoColour?: string;
}

const {
	bgColour = "hsl(var(--navy))",
	linkColour = "hsl(var(--white))",
	dividerColour = "hsl(var(--accent))",
	logoColour = "hsl(var(--teal))",
} = Astro.props;
---

<header>
	<nav>
		<ul>
			<li class="main-nav__li--home">
				<a href="/">
					<Icon name="day-eight-logo" class="day-eight-logo" aria-hidden="true" />
					<span class="visually-hidden">Home</span>
				</a>
				<!-- <a href="">Home</a> -->
			</li>
			<li><a href="/about">About</a></li>
			<li><a href="">Work</a></li>
			<li><a href="/contact">Contact</a></li>
		</ul>
	</nav>
</header>

<style define:vars={{ bgColour, linkColour, dividerColour, logoColour }}>
	header {
		/* background-color: hsl(var(--navy)); */
		background-color: var(--bgColour);
		/* border: 1px solid fuchsia; */
		/* padding: 2rem; */

		position: sticky;
		top: 0px;

		margin-block-start: calc(100vh - var(--header-height));
		z-index: 3;

		display: flex;
		align-items: center;

		padding-inline: 2rem;

		min-height: 5rem;
	}

	nav ul {
		list-style: none;
		padding: 0;
		display: flex;
		align-items: center;
		gap: 1rem;
		color: hsl(var(--white));
	}

	nav li {
		/* border: 1px solid fuchsia; */
	}

	/* nav li:not(:first-of-type, :last-of-type)::after { */
	nav li:not(:first-of-type, :last-of-type)::after {
		content: "|";
		/* display: block; */
		margin-inline-start: 1rem;
		color: hsl(var(--accent));
	}

	nav .main-nav__li--home {
		color: hsl(var(--accent3));
	}

	nav li a {
		/* color: hsl(var(--white)); */
		color: var(--linkColour);
		text-decoration: none;
	}

	nav .main-nav__li--home a {
		/* color: hsl(var(--white) / 33%); */
		color: var(--logoColour);
		/* color: hsl(var(--accent)); */
		font-size: 2rem;
		margin-inline-end: 2rem;
	}

	nav .main-nav__li--home a:is(:hover, :focus-visible) {
		color: hsl(var(--white) / 88%);
		/* color: hsl(var(--white)); */
	}
</style>
